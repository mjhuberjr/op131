#extend("master")
#export("section") {
    #loop(section.block, "block") {
    <h1> #(block.title) </h1>
    <h2> #(block.subtitle) </h2>
    <h3> #(block.description) </h3>
    <ul>
        #loop(section.links.0, "link") {
        <li> #(link.title) - #(link.url) </li>
        }
    </ul>
    }
}
